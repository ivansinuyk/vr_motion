---
description: Project context and safe working rules for VR motion analysis
alwaysApply: true
---

# VR Motion Analysis Project

This repo analyzes golf-stick swing motion from preprocessed video sessions. A valid session folder contains `mediapipe_data_full.json` with top-level `values` plus `video_processed.mp4`.

Core flow:
- `loader.py` loads a session.
- `main.py` runs interactive OpenCV playback and exports `swing_analysis.csv`.
- `swing_analyzer.py` is the main analysis engine.
- `config.py` defines `realtime` and `scientific` filter profiles; `set_filter_profile()` mutates module globals.
- `analysis.py`, `kalman.py`, `rts_smoother.py`, and `utils_filter.py` implement kinematics, Kalman filtering, RTS smoothing, despiking, and trajectory smoothing.
- `drawing.py` and `stats_overlay.py` render overlays.

Main commands:
- `python main.py --folder "<session>" --profile scientific`
- `python batch_article_evaluation.py --dataset-root "<dataset_root>" --out-dir "article_package/evaluation_outputs"`
- `python visualize.py --csv swing_analysis.csv`
- `python evaluate_filters.py`
- `python parameter_sweep.py --folder "<session>"`

Research/manuscript context:
- **Master sync:** `article_package/research_publication_status.md` — Article 1/2 status, artifacts, future directions.
- `article_package/` contains HAIT manuscript drafts, final DOCX candidates, evaluation CSVs, figures, and planning docs.
- **Article 1** (method pipeline): `article_package/Стаття_Аспірант_Синюк_HAIT_aligned_final_v5.docx` (format-accepted; refs 23-30 cited in-text, RC/CV and smoothness-index definitions, AI-use in declarations). Prior versions `..._aligned_final_v4.docx` and `..._wordsafe_v3.docx` kept for reference.
- **Article 2** (reference agreement + perturbation sensitivity): text source `article_package/second_article_manuscript.md`; **current DOCX** `article_package/Стаття_Аспірант_Синюк_HAIT_article2_v4.docx`; analysis bundle `second_article_outputs/v3/`; frozen fallbacks `article2_v2.docx`, `article2_v3.docx`. Builder: `build_second_article_docx.py`. Runbook (completed): `article2_post_annotation_publication_runbook.md`.
- `article_package/fix_hait_formatting.py` is the reusable HAIT formatter for Article 1 style; Article 2 uses `build_second_article_docx.py` (clone of v5 template). HAIT recipe: `second_article_plan_and_prompt.md` § HAIT Formatting Requirements.
- Fig. 2 (Article 1) is illustrative only; Article 2 Fig. 2 is a schematic annotation frame.

Scientific caution:
- Raw markerless landmarks are not ground truth.
- Diagnostic validation/sensitivity/ablation outputs should not be overclaimed as controlled accuracy evidence.
- Smoothness index and path efficiency are comparatively stable; derivative-heavy metrics are exploratory under heterogeneous videos.

Working cautions:
- `main.py` has top-level execution and should not be imported casually.
- `main.py` writes `swing_analysis.csv` to the current working directory.
- Avoid overwriting user DOCX files that may be open in Word; save new versions if locked.
- When editing DOCX programmatically, do not assign text to runs containing drawings, or embedded images can disappear. In particular, modify the "About the Authors" table in place; never delete and recreate it, or the photo cells are lost.
- Do not commit Office lock files (`~$*.docx`), pycache, or temporary extraction artifacts.
